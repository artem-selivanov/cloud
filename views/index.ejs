<!DOCTYPE html>
<html lang="uk">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Форма введення</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <div class="container mt-5">
        <h1 class="text-left mb-4">Форма введення для Cloudflare</h1>

        <!-- Контейнер для алертів -->
        <div id="alertContainer"></div>

        <form action="/process-form" method="POST" id="cloudflareForm">
            <div class="mb-3">
                <label for="email" class="form-label">Email (авторизація в Cloudflare):</label>
                <input type="email" class="form-control" id="email" name="email" required>
            </div>

            <div class="mb-3">
                <label for="apiKey" class="form-label">API Key (глобальний API-ключ Cloudflare):</label>
                <input type="text" class="form-control" id="apiKey" name="apiKey" required>
            </div>

            <div class="mb-3">
                <label for="domains" class="form-label">Список доменів:</label>
                <textarea class="form-control" id="domains" name="domains" rows="4" required></textarea>
            </div>

            <div class="mb-3">
                <label for="ipAddresses" class="form-label">IP-адреси для запису A:</label>
                <input type="text" class="form-control" id="ipAddresses" name="ipAddresses" required>
            </div>

            <div class="mb-3">
                <label for="settings" class="form-label">Налаштування:</label><br>
                <div class="form-check">
                    <input type="checkbox" class="form-check-input" id="deleteOldRecords" name="settings[deleteOldRecords]">
                    <label for="deleteOldRecords" class="form-check-label">Видалення старих DNS-записів</label>
                </div>
                <div class="form-check">
                    <input type="checkbox" class="form-check-input" id="clearCache" name="settings[clearCache]">
                    <label for="clearCache" class="form-check-label">Очистка кешу</label>
                </div>
                <div class="form-check">
                    <input type="checkbox" class="form-check-input" id="useHttps" name="settings[useHttps]">
                    <label for="useHttps" class="form-check-label">Використання HTTPS</label>
                </div>
            </div>

            <button type="submit" class="btn btn-success w-100">Надіслати</button>
        </form>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
    <script src="/js/validate.js"></script>
</body>
</html>
